@model IEnumerable<AppTest.Models.Rubrica>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.textoRubrica)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nivelDesarrollo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.aporteNota)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.textoRubrica)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nivelDesarrollo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.aporteNota)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.rubricaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.rubricaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.rubricaID })
        </td>
    </tr>
}

</table>
